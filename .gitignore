# dipendenze
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# produzione
/build
/dist

# env
.env
.env.local
.env*.local

# dati veri di seed (cibi e scheda personali) -- vedi src/lib/seed-data.example.ts
/src/lib/seed-data.local.ts

# capacitor
/ios
/android

# varie
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel

# Skill installate da strumenti esterni (per esempio uipro-cli): sono
# strumenti locali di chi sviluppa, non codice del progetto. Pesano un mega
# di CSV e script altrui, e la repo e' pubblica.
# Le impostazioni del progetto, se servissero, stanno in .claude/settings.json
# e quelle si committano.
.claude/skills/
